# Example backend environment variables (DO NOT commit real secrets)
# Copy this file to .env and fill in the real values locally or in your host's secret manager.

# MongoDB connection string (replace <PASSWORD_ENCODED> and <DBNAME>)
# If your password has special chars, URL-encode it (use encodeURIComponent in JS).
MONGODB_URI=mongodb+srv://yogesh:Yogesh4732@resume-rag.zs7xaiw.mongodb.net/resume_rag?retryWrites=true&w=majority

# JWT secret used to sign tokens (choose a long random string)
JWT_SECRET=replace_with_a_strong_secret_here

# Token expiry (example: 7d, 24h)
JWT_EXPIRES_IN=7d

# Upload path for temporary file storage in container
UPLOAD_PATH=/tmp/uploads

# Optional Redis URL for rate-limiting / idempotency (leave empty to use in-memory fallback)
# Example: redis://:password@redis-host:6379
REDIS_URL=

# Port (Render and many hosts set PORT automatically; you can leave this blank locally)
PORT=5000
